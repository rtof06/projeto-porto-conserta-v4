export interface CentrosAutomotivos {
  id: number;
  name: string;
  neighbourhood: string;
  uf: string;
  street: string;
  phone: string;
  atendimento: string;
};

export interface Servico {
  id: number;
  nome: string;
};